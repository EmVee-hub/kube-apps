apiVersion: v1
kind: Service
metadata:
  name: hello-page
  labels:
    app: hello-page
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hello-page